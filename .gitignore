# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor/tmp
*.tmp
*.swp
*~

# Node (if any local tooling gets added)
node_modules/

# agent-browser 로컬 상태(사용자 홈에 있으나 혹시 프로젝트로 복사될 경우 대비)
.agent-browser/

# 한글(HWPX) 작업 파일 — 다른 세션에서 작업 중이므로 이 저장소에 포함하지 않음
*.hwpx

